import { Plugin } from 'vue';
declare global {
    interface Window {
        readonly tidioChatApi: {
            on: (eventName: string, callback: () => void) => void;
        };
    }
}
declare const VueTidio: Plugin;
export default VueTidio;
